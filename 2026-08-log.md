### Day 1: Environment Setup
- Installed VirtualBox (solved several issues on company PC)
- Downloaded and installed Ubuntu 26.04 LTS
- Configured VM settings (Memory: 4096MB, CPU: 2)
- Changed timezone to JST

**Command used:**
`sudo timedatectl set-timezone Asia/Tokyo`


### Day 2: Basic Commands
**Command used:**
- `ls -la`: list files (detailed + hidden files)
- `cd ~`: go to home directory
- `mkdir`: create folder
- `touch`: create empty file
- `cp`: copy
- `mv`: move or change name
- `rm`: delete
- `rm -r`: delete folder


### Day 3: GitHub Account Setup & Documentation
- Created GitHub account to record learning progress
- Created this README.md to document
  
**Command used:**
- `echo`: display and write text
- `sudo`: execute as root
- `whoami`: confirm the current username
- `cd ..`: go up one directory
- `rmdir`: delete folder
- `mv * ..`: All files move up one directory
- Lerned that no space for naming


### Day 4: Ubuntu setting and Command practice
- Changed the clipboard sharing setting in the device settings to 'Bidirectional' so that I could copy documents from Windows.
- Reviewed how to write to files, delete them, and so on.
- Changed the VirtualBox host key combination to Right Alt, since the default was Right Ctrl, which this PC does not have.
- installed the necessary packages to enable Japanese input on Ubuntu.
  
**Command used:**  
- `sudo apt update`
- `sudo apt install ibus-mozc`
- `sudo reboot`
- `head`: Displayed only the first 10 lines of a file.
- `tail`: Displayed only the last 10 lines of a file.
- `nano`: Edited a file in the terminal. ー＞Save: Ctrl + O ー＞Enter / ー＞Exit: Ctrl + X
- `chmod 644 *`: Changing File Permissions / Other users can only read the files in the folder.
- `head -n3`:  Displayed only the first 3 lines of a file.
- `grep`:  Searching for a specific string  Example: 'grep Day 10_Aug_learning.txt' (searching for the word "Day")

### Day 5: Command practice using https://labex.io/labs
- Deep Dive into the `ls` command `ls -a` `ls -h` `ls -l` `ls -alh` `ls -lt` `ls -ltr` `ls --color=never` `ls --color=always` `ls | cowsay` 
- `man ls`: For seeing a complete list of options and detailed explanations.
- `File.txt` `file.txt` `FILE.txt` are all different files in Linux
- `echo`: 「echo "letters"> file-name」is overwrite.
- `echo`: 「echo "letters">> file-name」This is appended to the existing document rather than overwriting it.
- `echo -e "1 line\n2 line\n3 line"`: How to break lines 
- `echo "Input letters" (Using enter to break line) ending with ">> file name`: Another way to break line 
- `cat>> file_name<<EOF` ending with `EOF`: Using cat command also can input letters. My favorite way to break line


### Day 6: Command practice 
- Installed tree command
- Review the commands already learnt.


### Day 7: Ubuntu error fixing & Learning with Bandit
When I switch to Ubuntu, the caps lock key starts blinking and I cannot input anything.
So I fixed the Windows settings and entered the commands in PowerShell.

- Opened PowerShell as an administrator. (Right-click on the app icon)
- Entered `dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart`
- Confirmed the installation was complete, entered the commands below.
- `dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart`
- After confirming that both commands were complete, entered `Restart-Computer` to restart the computer.
- Opened PowerShell as an administrator by right-click on the app icon.
- Entered the command `wsl --update`
- Waited for a moment until the update finished
- Entered `wsl --status` to check the current version and other details.

- Using Bandit(https://overthewire.org/wargames/bandit/bandit2.html) for studying
- Using `ssh` to login to bandit game  `ssh bandit0@bandit.labs.overthewire.org -p 2220`
  ↑ これはBandit Level 0の場合の書き方。Level 1になると、`ssh bandit1@bandit.labs.overthewire.org -p 2220`
- `cat ./-` これは「-」のファイルを表示させる場合のコマンド。`ls -l`や`cat -`では表示されない。-のときは`./` または`--`を前につける 
-  
